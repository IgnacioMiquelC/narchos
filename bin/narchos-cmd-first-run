#!/bin/bash

set -e

FIRST_RUN_MODE=~/.local/state/narchos/first-run.mode

if [[ -f "$FIRST_RUN_MODE" ]]; then
  rm -f "$FIRST_RUN_MODE"

  bash "$NARCHOS_PATH/install/first-run/battery-monitor.sh"
  bash "$NARCHOS_PATH/install/first-run/firewall.sh"
  bash "$NARCHOS_PATH/install/first-run/gnome-theme.sh"
  sudo rm -f /etc/sudoers.d/first-run

  bash "$NARCHOS_PATH/install/first-run/wifi.sh"
  bash "$NARCHOS_PATH/install/first-run/welcome.sh"
fi
